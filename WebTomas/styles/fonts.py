import reflex as rx
from enum import Enum

class font(Enum):
    LOGO = "Noto Serif"
    DEFAULT = "Poppins"
    TITLE = "Poppins"

class FontWeight(Enum):
    LIGHT = "300",
    BOLD =  "700",
    DEFAULT = "400"
